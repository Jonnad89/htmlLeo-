Consigna: Clasificador de Películas Favoritas
El objetivo es crear una lista dinámica de películas donde el usuario pueda agregar títulos, verlos numerados y usar botones para reordenar la lista, simulando un sistema de votación o de prioridades.

Gestión del Array: Mantener un array global (let peliculas = []) de strings.Manipulación básica de arrays (.push()).2.Función renderizarLista(): El corazón del proyecto. Debe recorrer el array de peliculas y generar la lista HTML (con el botón Subir y Bajar) cada vez que el array cambie.Manipulación del DOM, uso de .innerHTML = '' para limpiar y document.createElement.3.Funcionalidad "Subir" y "Bajar": Crear funciones que permitan mover un elemento (película) dentro del array. Usar la lógica de intercambio de índices ([i] = [i-1]; [i-1] = temp;) para reordenar.Lógica de intercambio de elementos dentro de un array y manejo de índices (i e i ± 1).4.Control de Límites: Los botones Subir y Bajar deben deshabilitarse (o no funcionar) si la película está en la primera o última posición, respectivamente.Control de flujo (if en los límites del array).