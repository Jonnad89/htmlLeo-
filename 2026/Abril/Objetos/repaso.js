// completar

class Producto {
    constructor(nombre, precio){
        // Completá acá
    }

    // El Getter convierteel precio a un formato lindo
    get precioFormateado(){
        return `$${this.precio.toLocalString('es-AR')}`
    }
}

const tele = //completá acá

console.log(/* Completá acá */);
