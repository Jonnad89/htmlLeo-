// Forma tradicional 
function sumar (a, b) {
    return a + b;
}

// Forma moderna (arrow function)
const summarArrow = (a, b) => a + b;

// Ejemplo con 1 solo parámetro (no lleva paréntesis)
const duplicar = n => n * 2;

console.log(summarArrow(5,10))
console.log(duplicar(8))